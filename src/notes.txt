Need to understand MVCC Snapshots in b-tree and pages

Need to understand what bitmap scans are

Need to understand how VACUUMs work

Need to understand ascending logic and locking

Need to understand file offsets and blocks

Need to understand how pins work and how they're different from latches

Need to understand what suffix truncation is
- Suffix truncations is taking a range of sub-keys and removing the trailing bytes at the first point they different
- this is used to store separator keys
- Prefix compression compresses keys stored downward
Example suffix truncation = apple_green, apple_red -> apple_r (which would be the high_key/separator in the parent)

Need to understand split page logic - byte aware splits?

// Need pin logic for internal pages - if pinned directly address from arena, if not then go through cache
// Need better lock for internal maybe optimistic version locking


// Confirmed that there is one cache for pages UNLESS we pin to memory
// Confirmed there is one lock for pages which resides in the cache/mapping table UNLESS we pin to memory

// Steps forward is to have 3 elements [Mapping table - Buffer, PageManager] PageManager will coordinate and wire up the mapping table with the buffer
// Calling code such as tree will only interact with the PageManager

//

TODO

- We also need to do some work with the Slot Array as we know how many cells we have and their sizes so we can apply some logic to determine good splits and return good errors or context based on this
- Then we need to define good tests

// TODO Want to look at how we handle re-traversing to a level if we are propagating a split and the tree has changed invalidating our path
// TODO Want to look at concurrent traversal and reading pages etc --> Where do we want to do that

// TODO Implement Binary Search on slot array - if we have more than a slot_count which warrants it
// TODO Implement a get split index on the page specific layers which returns a split_index(ctx?) and then passes to a split handler to return a range
// and pass to a trnasfer on the slotted page mut - but we need to have been given a new page by the tree(op_ctx -> buffer manager)

// Need to think about Comparitors for the b-tree and how we do that

// Need to make compact methods
// Need to make insert method which checks contiguous space then fragmented and compacts if needed or return split error/ctx
// Need to make a split plan method which returns a split plan for us to use
// Need to make page specific split methods which carry out a split plan and apply page specifics to a split returning ctx to the tree
// Need to also include an increment fragment count to where we do split and transfer
